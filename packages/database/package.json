{
  "name": "@bigbazar/db",
  "version": "1.0.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "db:generate": "npx prisma generate",
    "db:push": "npx prisma db push",
    "db:seed:admin": "npx ts-node seed-admin.ts",
    "db:seed:categories": "npx ts-node seed-categories.ts",
    "db:import:products": "npx ts-node import-products.ts"
  },
  "dependencies": {
    "@tidbcloud/serverless": "^0.3.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
