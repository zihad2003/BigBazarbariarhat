{
  "name": "@bigbazar/db",
  "version": "1.0.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "scripts": {
    "db:generate": "prisma generate",
    "db:push": "prisma db push",
    "db:seed:admin": "ts-node seed-admin.ts",
    "db:seed:categories": "ts-node seed-categories.ts",
    "db:import:products": "ts-node import-products.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.10.0",
    "@prisma/extension-accelerate": "^1.0.0"
  },
  "devDependencies": {
    "prisma": "^5.10.0",
    "typescript": "^5.0.0"
  }
}
